# Secrets — never commit
.env
.env.zsh
**/secrets/
**/*credentials*
**/*.key
**/*.pem

# SSH: only the config file is allowed in this repo. Block everything else.
ssh/.ssh/*
!ssh/.ssh/config

# OS / editor cruft
.DS_Store
*~
*.swp
.idea/
.vscode/
